<nav aria-label="Pagination">
	<ul class="pagination">
		{% for page in pagination.pages %}
			<li class="page-item{% if page.active %} active{% endif %}" aria-label="{{ page.label }}">
				<a class="page-link" href="{{ page.url }}">{{ page.label }}</a>
			</li>
		{% endfor %}
	</ul>
</nav>
